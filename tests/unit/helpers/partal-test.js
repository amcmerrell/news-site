
import { partal } from 'bustle-clone/helpers/partal';
import { module, test } from 'qunit';

module('Unit | Helper | partal');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = partal([42]);
  assert.ok(result);
});

